dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        jcenter()
        maven { url 'https://jitpack.io' }
        google()
        mavenCentral()
    }
}
rootProject.name = "QRScanner"
include ':app','switchdatetime','simplerecyclerview','simplecropview','flagkit','compressor','custom_qr_generator','gridcolor','dragtoclose','multipletype','colorpicker'